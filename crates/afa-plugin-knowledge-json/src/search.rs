//! Code Map: Phase 0 search stub.
//!
//! Phase 0 placeholder for the search / scoring
//! helpers (tokenize free_text, score a record,
//! rank candidates). Phase 2 populates.
//!
//! Story (plain English): The search layer is
//! the part of the adapter that turns a
//! `FindInformationRequest` into a ranked
//! `FindInformationResponse`. Phase 0 only
//! needs the module to exist; Phase 2 fills
//! in the real scoring logic.

#[cfg(test)]
mod tests {
    #[test]
    fn search_module_compiles() {
        // Phase 0 placeholder test: the module
        // exists and compiles. Phase 2 will
        // replace this with the per-method
        // search tests (tokenize, score, rank).
    }
}
